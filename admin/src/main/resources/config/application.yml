spring:
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/edgetool?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
redis:
  host: 127.0.0.1
  database: 0
  port: 6379
  password:
  cache:
    cache-names: menus_cache
server:
  port: 8081
  compression:
    enabled: true
spring.servlet.multipart.max-file-size: 100MB
spring.servlet.multipart.max-request-size: 300MB